# Aeltra Outbound · Docker Compose para EasyPanel (o cualquier host con Docker).
# El server + worker de envío paceado + scheduler de rutinas corren en el MISMO proceso.
#
# En EasyPanel: creá un servicio tipo "Compose", apuntá a este archivo y cargá las
# variables (las de ${...}) en la pestaña Environment del servicio. NO subas el .env.
services:
  aeltra:
    build:
      context: .
      dockerfile: Dockerfile
    restart: unless-stopped
    # EasyPanel rutea al puerto interno vía su proxy (configurá el dominio → puerto 8000).
    # Sin container_name ni mapeo de puertos al host, para no chocar con otros servicios.
    expose:
      - "8000"
    environment:
      # ── Envío por SMTP (Zoho, dominio propio autenticado) ──
      SENDER_BACKEND: "${SENDER_BACKEND:-smtp}"
      SMTP_HOST: "${SMTP_HOST:-smtppro.zoho.com}"
      SMTP_PORT: "${SMTP_PORT:-465}"
      SMTP_USER: "${SMTP_USER}"
      SMTP_PASS: "${SMTP_PASS}"
      FROM_NAME: "${FROM_NAME:-Aeltra}"
      FROM_EMAIL: "${FROM_EMAIL}"
      PLAIN_TEXT_ONLY: "${PLAIN_TEXT_ONLY:-true}"
      # ── Lectura de respuestas por IMAP (mismo buzón Zoho) ──
      IMAP_HOST: "${IMAP_HOST:-imappro.zoho.com}"
      IMAP_PORT: "${IMAP_PORT:-993}"
      IMAP_USER: "${IMAP_USER}"
      IMAP_PASS: "${IMAP_PASS}"
      # ── Prospección ──
      PROSPECTS_MOCK: "${PROSPECTS_MOCK:-false}"
      APIFY_TOKEN: "${APIFY_TOKEN}"
      GOOGLE_MAPS_API_KEY: "${GOOGLE_MAPS_API_KEY}"
      # ── Seguridad / compliance (IMPORTANTES en producción) ──
      BASIC_AUTH_USER: "${BASIC_AUTH_USER}"
      BASIC_AUTH_PASS: "${BASIC_AUTH_PASS}"
      UNSUB_BASE: "${UNSUB_BASE}"          # ej: https://tu-dominio/api/baja  (NO localhost)
      # ── Ritmo ──
      DEFAULT_WINDOW_HOURS: "${DEFAULT_WINDOW_HOURS:-6}"
      DAILY_SEND_CAP: "${DAILY_SEND_CAP:-40}"
      # ── Base de datos ──
      # Si DATABASE_URL está seteada (Postgres/Supabase), usa Postgres; si no, SQLite en el volumen.
      DATABASE_URL: "${DATABASE_URL}"
      DB_PATH: "/data/aeltra.db"
    volumes:
      - aeltra-data:/data                  # SQLite persiste entre reinicios/deploys
    healthcheck:
      test: ["CMD", "python", "-c", "import urllib.request; urllib.request.urlopen('http://127.0.0.1:8000/api/health')"]
      interval: 30s
      timeout: 5s
      retries: 3
      start_period: 20s

volumes:
  aeltra-data:
